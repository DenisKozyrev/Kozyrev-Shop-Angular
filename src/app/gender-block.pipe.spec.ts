import { GenderBlockPipe } from './gender-block.pipe';

describe('GenderBlockPipe', () => {
  it('create an instance', () => {
    const pipe = new GenderBlockPipe();
    expect(pipe).toBeTruthy();
  });
});
